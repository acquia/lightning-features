<?php
/**
 * @file
 * Code for the assemble_metatags feature.
 */

include_once 'assemble_metatags.features.inc';

/**
 *  Implements hook_post_features_enable_feature().
 */
function assemble_metatags_post_features_enable_feature($component) {
  // Log components as they are enabled.
  watchdog('component', 'assemble_metatags enabled ' . $component . ' component');
}

/**
 * Implements hook_entity_info_alter().
 */
function assemble_metatags_entity_info_alter(&$entity_info) {

  // Alternate Preview view mode for files.
  $entity_info['file']['view modes']['alt_preview'] = array(
    'label' => t('Alternate Preview'),
    'custom settings' => TRUE,
  );

}

